# Frontend Rules

- Preserve readability at 375px mobile width and desktop width.
- Avoid generic placeholder UI; choose a clear visual direction.
- Do not add frontend dependencies unless the PRD justifies them.
- Include screenshot evidence for UI changes when browser tooling is available.
- Verify key text and interactions with static checks, browser checks, or both.
