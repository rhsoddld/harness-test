# Security

セキュリティ判断をエージェントが誤推測しないよう、境界と禁止事項を明文化する。

## Baseline Rules

- 秘密情報をリポジトリへ書き込まない。
- 外部入力は境界で検証する。
- 権限や token の扱いは docs またはコードコメントで所有者と用途を明記する。
- GitHub plugin や `gh` を使う操作では、公開範囲と副作用を確認してから実行する。
