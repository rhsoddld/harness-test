# Packages

Shared code lives here when multiple apps need it.

## Layout

- `packages/ui`: shared UI primitives and presentation helpers.
- `packages/config`: shared lint, TypeScript, environment, and test configuration.
